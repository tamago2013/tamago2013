# sokuiki raw data ssm name
sokuiki-raw-ssm-name=scan_data2d

# sokuiki raw data ssm id
sokuiki-raw-ssm-id=0

# odometry data ssm name
odometry-ssm-name=spur_odometry

# odometry data ssm id
odometry-ssm-id=0

# sokuiki accumulated data ssm name
sokuiki-accumulated-ssm-name=sokuiki_ac

# accumulating interval distance[m]
accumulating-interval-distance=0.200000

# accumulating maximum distance[m]
accumulating-max-distance=1.000000

